const numbers = [1, 5, 6, 10, 45, 2, 7];
// numbers.forEach((x) => console.log(x));

// Multi line parameter
numbers.forEach((num) => {
  const doubled = num * 2;
  console.log(doubled);
});

// ----> ForEach hocche amar prottekta element er jnno kaj korbo but return dorker nai
// ----> Return dorker hole map use korbo oita amake return value dibe
